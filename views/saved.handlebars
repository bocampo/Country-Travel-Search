{{!-- {{!-- {{#each savedCountry as |savedCountry| }}
<div class="row mb-4 SavedCountry">
  <div class="col-md-5">
    <h2>
      Saved countries:
      </h2>

  </div>
</div>
{{/each}} --}}


<h1>Saved Countries:</h1>
<table>
    <tbody>
        {{#each data}}
            <tr>
                <td>{{this.name}}</td>
            </tr>
        {{/each}}
    </tbody>
</table>

<script src="../public/js/saved.js"></script>


<!--
<div>
  <p>Saved Countries</p>
  <div id="1"><button class="delete" Delete></button></div>
  <div id="2"><button class="delete">Delete</button></div>
  <div id="3"><button class="delete">Delete</button></div>
  <div id="4"><button class="delete">Delete</button></div>
  <div id="5"><button class="delete">Delete</button></div>


</div>


<div>
  <p>Saved Countries</p>
  {{#each countries}}
  <div id="{{@index}}">
    <button class="delete">Delete</button>
    <span>{{this}}</span>
  </div>
  {{/each}}
</div>

-->